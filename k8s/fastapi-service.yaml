apiVersion: v1
kind: Service
metadata:
  name: fastapi-iot-service
spec:
  type: NodePort
  selector:
    app: fastapi-iot-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080